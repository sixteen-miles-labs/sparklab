{
  "name": "replace-with-one-swe-bench-instance",
  "workspace": "/absolute/path/to/checked-out/repository",
  "steps": [
    {"argv": ["opencode", "run", "{prompt}"], "prompt": "First scripted user turn."},
    {"argv": ["opencode", "run", "--continue", "{prompt}"], "prompt": "Second scripted user turn."},
    {"argv": ["opencode", "run", "--continue", "{prompt}"], "prompt": "Third scripted user turn."}
  ],
  "evaluator": {
    "argv": ["python", "/absolute/path/to/swebench_evaluator.py", "--workspace", "{workspace}"],
    "expected_exit_code": 0
  },
  "metadata": {
    "swebench_instance_id": "required",
    "reference_patch_sha256": "required"
  }
}
