# Quality benchmark results

This directory stores immutable JSON outputs from `run_quality.py`. A filename should
identify the model, weight format, workload, and run date, for example:

`qwen3.6-35b-a3b-nvfp4-w1-smoke-20260831.json`

Smoke results validate the harness only; do not compare them with the paper's full runs.
